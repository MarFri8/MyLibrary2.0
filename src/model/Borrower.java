package model;

public class Borrower extends User {

    public Borrower(int id, String firstName, String lastName, String email, String password, String type) {
        super(id, firstName, lastName, email, password, type);
    }
}
